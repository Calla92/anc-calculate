import { ChargeCalculator } from "@/components/calculator/ChargeCalculator";

export default function IndexPage() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-gray-50 to-blue-50 p-2 md:p-6">
      <ChargeCalculator />
    </div>
  );
}
